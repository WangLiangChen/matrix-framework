spring:
  application:
    name: matrix-shop-product
  datasource:
    url: jdbc:h2:mem:matrix-shop-product;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

server:
  port: 8081
