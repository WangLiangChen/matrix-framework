package ${adapterClientPackage};


import ${portClientPackage}.${portClientName};
import org.springframework.stereotype.Component;

/**
 * @author ${author} ${.now?string('yyyy-MM-dd HH:mm:ss')}
 */
@Component
public class ${adapterClientName} implements ${portClientName} {
}